{
  "name": "mathning",
  "private": true,
  "workspaces": [
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @mathning/shared",
    "dev:mobile": "npm run build:shared && npm run start -w mobile"
  }
}
